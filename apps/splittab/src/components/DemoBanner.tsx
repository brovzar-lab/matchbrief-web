export default function DemoBanner(): JSX.Element {
  return (
    <div className="bg-amber-400 text-amber-900 text-center text-xs font-semibold py-1.5 px-4 sticky top-0 z-50">
      Demo Mode — data is not saved
    </div>
  );
}
