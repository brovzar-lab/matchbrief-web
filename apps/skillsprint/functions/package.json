{
  "name": "skillsprint-functions",
  "private": true,
  "version": "0.1.0",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "test": "vitest run"
  },
  "engines": { "node": "20" },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.27.0",
    "firebase-admin": "^12.1.0",
    "firebase-functions": "^4.8.0"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "vitest": "^2.1.8"
  }
}
